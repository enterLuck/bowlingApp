function calculationChecker(frameNum){
	if(frameNum>1){
	var previousFrameNum = frameNum - 1;
	var beforePreviousFrameNum = frameNum - 2;
	var b4 = (fr["frame"+beforePreviousFrameNum+"strike"] == "X");
	var p1 = (fr["frame"+previousFrameNum+"spare"] == "/");
	
	switch(frameNum) {
    case :
        code block
        break;
    case n:
        code block
        break;
    default:
        default code block
}
}
}